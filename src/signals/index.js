'use strict';

const { WaveSignal, WAVE_DEFAULTS } = require('./wave');
const { PhaseSignal, PHASE_TYPES } = require('./phase');

module.exports = { WaveSignal, WAVE_DEFAULTS, PhaseSignal, PHASE_TYPES };
